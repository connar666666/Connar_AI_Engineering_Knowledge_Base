# 未完成问题与下一步

本文件帮助新工作空间快速判断哪些问题尚未闭环，避免把设计讨论误认为已经完成。

## 1. Email MCP

### 高优先级

- 读取 `Email_MCP` 仓库指定分支下 `emails/` 的真实结构。
- 找到 MCP Server 启动入口和 Tool 注册位置。
- 列出当前 Tool List、缺失功能和测试覆盖。
- 检查 `prompts/skills` 是否具备完整工作流、确认点和失败分支。
- 与 fork 的 `mcp-email-server` 比较 thread、draft、附件和状态管理能力。

### 外部依赖

等待邮箱服务器团队确认：

- IMAP 扩展。
- 原生 thread / conversation 能力。
- 并发与频率限制。
- 统一授权和 SSO 映射。
- 发送和审计策略。

## 2. SZU_TK / Transfer Agent

### 高优先级

- 从真实代码确认 Agent View JSON 的构造位置。
- 验证每轮是否包含历史状态累积。
- 找到 Snapshot 保存、上下文拼接和 LLM 调用链。
- 找到四个前端模板的 Mock 数据位置。
- 设计从全量 JSON 生成迁移到 operation-based 输出的修改方案。

### 验证指标

- 每轮输入 token。
- KV Cache 前缀命中情况。
- 延迟与成本。
- 引用旧元素的错误率。
- 多轮任务完成率。
- 摘要造成的长期约束丢失率。

## 3. AAAI 可读性项目

### 高优先级

- 读取 `run_lbf_rule_discover_shaping.sh` 的完整命令链。
- 跟踪 Python 主入口、配置和产物目录。
- 确认 Rule Discovery 的表示、候选生成、评分和筛选。
- 确认 Reward Shaping 的真实公式与训练接入位置。
- 建立代码—论文—实验结果映射表。

### 待实验验证

- 可读性是否提升。
- 是否损害任务性能。
- 不同 seed 的稳定性。
- 对不同环境规模的泛化。
- 规则和 shaping weight 的消融。

## 4. OpenAI Agent Attack

- 继续读取比赛 notebook 和当前实现。
- 明确单次与多轮 Prompt 发送规则。
- 明确候选去重和相似度限制。
- 定义成功判定与 Replay Verification。
- 将向量邻域搜索和成功样本变异做成可测试实验。
- 完成完整升级设计文档和状态机。

## 5. ComfyUI WorkBench / FrameWeave

- 对照最新代码验证数据库 V3 是否落地。
- 找出 Project / Canvas 邀请相关前后端代码。
- 确认首页邀请通知是否实现。
- 检查 Remotion 胶水代码和人物一致性方案。
- 整理真实启动链路、Compose、Redis、数据库和 Worker。

## 6. 手撕代码

- 继续练习 LRU Cache + TTL 的完整输入输出版本。
- 补全链表区间反转和 k 组反转。
- 完成长回文子串的可手写版本。
- 用真实后端场景继续巩固 asyncio、线程池和进程池。
- 每道题记录边界条件、复杂度和常见错误。

## 7. 实习与面试

- 重新读取已有面试逐字稿。
- 按“问题—原回答—缺陷—优化回答—证据”逐题整理。
- 为 Transfer Agent 准备真实架构图和完整请求链路。
- 补充个人负责代码、测试和指标。
- 准备至少一个失败案例、定位过程和修复结果。

## 8. 工程环境

- 继续解决 WSL 中 Windows / Linux Node/npm 路径混用。
- 确认 Codex 插件的真实安装目录、manifest 和加载日志。
- 对 Docker 项目终端差异优先比较环境变量与 Docker Context。
- 将已验证有效的排障命令沉淀到 `Engineering/`，但不要记录密钥。

## 9. 恢复层本身的维护

出现以下情况时更新本文件：

- 任务已经完成：从开放列表移除，并在项目状态中写入已确认事实。
- 新代码推翻旧推测：更新核心决策状态。
- 新增长期项目：加入 `PROJECT_STATE_INDEX.md`。
- 只是补充普通知识：只更新项目文档，不扩张恢复层。

## 10. 下一次工作空间的首要动作

当用户在新工作空间开始某个项目时，不要一次处理所有开放问题。先根据用户当前问题选择一个项目，并完成：

```text
恢复项目状态
→ 读取相关技术文档
→ 检查最新真实代码或资料
→ 标记新发现与旧结论差异
→ 更新项目状态和开放问题
```